<nav class="navbar navbar-expand-lg navbar-dark bg-purple-800 shadow-sm py-3">
    <div class="container-fluid px-4">
        <a class="navbar-brand fw-bold fs-4" href="/">Encuentra tu Coche</a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center fs-5">
                <li class="nav-item"><a class="nav-link px-3" href="/">Inicio</a></li>
                <li class="nav-item"><a class="nav-link px-3" href="/mapa">Mapa</a></li>
                <li class="nav-item"><a class="nav-link px-3 me-4" href="/historial">Historial</a></li>
                
                <% if (typeof usuario !== 'undefined' && usuario) { %>
                    <li class="nav-item border-start border-secondary ps-4 d-none d-lg-block">
                        <span class="text-light me-4"><i class="bi bi-person-circle me-1"></i>Hola, <%= usuario %></span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-danger rounded-pill px-4" href="/logout" id="btn-logout">Cerrar Sesión</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="btn btn-primary text-white rounded-pill px-4" href="/login">Iniciar Sesión</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>